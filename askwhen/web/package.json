{
  "name": "askwhen-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The askwhen.me request page. One app, N pages; a slug selects a dump.",
  "scripts": {
    "types": "node gen-types.mjs",
    "typecheck": "npm run types && tsc && tsc -p tsconfig.test.json",
    "build": "npm run types && node build.mjs",
    "test": "node --test 'test/*.test.ts'",
    "check": "npm run typecheck && node build.mjs && npm test && node test/no-network.mjs"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "esbuild": "^0.25.0",
    "json-schema-to-typescript": "^16.0.0",
    "typescript": "^7.0.2"
  }
}
